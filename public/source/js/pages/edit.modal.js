
document.addEventListener('DOMContentLoaded', function () { // Open the modal when the page is ready
    var resignationModal = new bootstrap.Modal(document.querySelector('.special'), {});
    resignationModal.show();
});
